

# 训练配置
TRAIN_CONFIG = {
    "per_device_train_batch_size": 1,
    "gradient_accumulation_steps": 16,
    "learning_rate": 3e-7,
    "num_train_epochs": 1,
    "warmup_ratio": 0.1,
    "save_steps": 1000,
    "logging_steps": 100,
    "max_samples": 50
}